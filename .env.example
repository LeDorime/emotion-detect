# Only needed for the "Deep Analysis" serverless function (api/deep-analyze.ts).
# The live on-device detector needs no API key.
# In production, set this in the Vercel project (Settings -> Environment Variables
# or `vercel env add ANTHROPIC_API_KEY`).
ANTHROPIC_API_KEY=
